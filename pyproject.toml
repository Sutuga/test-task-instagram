[tool.pylint.format]
max-line-length = 90

[tool.pylint.messages]
disable = ["too-few-public-methods"]

[tool.black]
line-length = 90

[tool.pytest.ini_options]
addopts = [
    "--alluredir", "allure-results",
    "--clean-alluredir"
]